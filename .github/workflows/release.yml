name: Release

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  release:
    name: Release
    env:
      branch: temp-${{ github.run_number }}
    outputs:
      next: ${{ steps.semver.outputs.next }}
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0 # Fetch all history for tags
      - name: Check for significant changes
        id: changes
        run: |
          # Find the last valid semver tag (exclude major-only tags like v2)
          LAST_TAG=$(git tag --sort=-version:refname | grep -E '^v?[0-9]+\.[0-9]+\.[0-9]+$' | grep -vE '^v?[0-9]+$' | head -1)

          if [ -z "$LAST_TAG" ]; then
            echo "No previous valid semver tag found, proceeding with release"
            echo "has_significant_changes=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "Comparing against last tag: $LAST_TAG"

          # Get list of changed files, excluding non-significant paths
          CHANGED_FILES=$(git diff --name-only $LAST_TAG..HEAD | grep -vE '^\.github/workflows/' | grep -vE '^README\.md$' | grep -vE '\.md$' | grep -vE '^\.gitignore$' | grep -vE '^\.gitattributes$' | grep -vE '^renovate\.json$' | grep -vE '^CODEOWNERS$' || true)

          if [ -z "$CHANGED_FILES" ]; then
            echo "No significant changes detected (only workflow/README/docs changes)"
            echo "has_significant_changes=false" >> $GITHUB_OUTPUT
          else
            echo "Significant changes detected in:"
            echo "$CHANGED_FILES"
            echo "has_significant_changes=true" >> $GITHUB_OUTPUT
          fi
      - name: Get Semver
        id: semver
        if: steps.changes.outputs.has_significant_changes == 'true'
        continue-on-error: true
        uses: ietf-tools/semver-action@c90370b2958652d71c06a3484129a4d423a6d8a8 # v1
        with:
          token: ${{ github.token }}
          skipInvalidTags: true
      - name: Skip release (no significant changes)
        if: steps.changes.outputs.has_significant_changes == 'false'
        run: |
          echo "Skipping release: Only workflow, README, or documentation changes detected."
          echo "No code changes found that would warrant a new release."

      - name: Git Setup
        if: steps.semver.outputs.next
        run: |
          git config --global user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          git config --global user.name "${GITHUB_ACTOR}"
          git checkout -b ${{ env.branch }}

      - name: Rebuild ./dist
        if: steps.semver.outputs.next
        run: |
          npm run dist
          git add ./dist --force
          git commit -m "Rebuild dist/ directory"
          git push -u origin HEAD:${{ env.branch }}

      - name: Tag and Release (Major)
        if: steps.semver.outputs.next && steps.semver.outputs.nextMajor
        env:
          GH_TOKEN: ${{ github.token }}
          tag: ${{ steps.semver.outputs.nextMajor }}
        run: |
          if [ -z "${{ env.tag }}" ]; then
            echo "No major tag available, skipping major release"
            exit 0
          fi
          git tag -f ${{ env.tag }}
          git push -f origin tag ${{ env.tag }}

          if [ $(gh release view ${{ env.tag }} --json name -q .name) ]; then
            gh release edit ${{ env.tag }} --title ${{ env.tag }} --tag ${{ env.tag }}
          else
            # Get previous valid semver tag (exclude current tag and major-only tags like v2)
            PREV_TAG=$(git tag --sort=-version:refname | grep -E '^v?[0-9]+\.[0-9]+\.[0-9]+$' | grep -v "^${{ env.tag }}$" | grep -vE '^v?[0-9]+$' | head -1)
            if [ -n "$PREV_TAG" ]; then
              gh release create ${{ env.tag }} --title ${{ env.tag }} --notes "$(gh api repos/${{ github.repository }}/releases/generate-notes -f tag_name=${{ env.tag }} -f previous_tag_name=$PREV_TAG -q .body)"
            else
              gh release create ${{ env.tag }} --title ${{ env.tag }} --generate-notes
            fi
          fi

      - name: Tag and Release (Semver)
        if: steps.semver.outputs.next
        env:
          GH_TOKEN: ${{ github.token }}
          tag: ${{ steps.semver.outputs.next }}
        run: |
          git tag ${{ env.tag }}
          git push origin tag ${{ env.tag }}

          if [ $(gh release view ${{ env.tag }} --json name -q .name) ]; then
            gh release delete ${{ env.tag }} --yes
          fi

          # Get previous valid semver tag (exclude current tag and major-only tags like v2)
          PREV_TAG=$(git tag --sort=-version:refname | grep -E '^v?[0-9]+\.[0-9]+\.[0-9]+$' | grep -v "^${{ env.tag }}$" | grep -vE '^v?[0-9]+$' | head -1)
          if [ -n "$PREV_TAG" ]; then
            gh release create ${{ env.tag }} --title ${{ env.tag }} --notes "$(gh api repos/${{ github.repository }}/releases/generate-notes -f tag_name=${{ env.tag }} -f previous_tag_name=$PREV_TAG -q .body)"
          else
            gh release create ${{ env.tag }} --title ${{ env.tag }} --generate-notes
          fi

      - name: Cleanup
        if: always()
        run: |
          if [ "$(git ls-remote origin ${{ env.branch }})" ]; then
            git push --delete origin ${{ env.branch }}
          else
            echo "Cleanup not required"
          fi
